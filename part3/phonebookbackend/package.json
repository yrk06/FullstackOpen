{
  "name": "phonebookbackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../phonebook && npm run build && cp -r build ../phonebookbackend",
    "deploy": "cd ../../ && git subtree push --prefix part3/phonebookbackend heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"phonebook build\" && npm run deploy"
  },
  "author": "Yerik K.",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
